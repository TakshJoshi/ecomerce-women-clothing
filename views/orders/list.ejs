<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Order List</title>
</head>
<body>
    <h1>Order List</h1>
    <% if (orders.length > 0) { %>
        <% orders.forEach(order => { %>
            <div>
                <h2>Order ID: <%= order._id %></h2>
                <p>User: <%= order.user.name %></p>
                <p>Total: $<%= order.total %></p>
                <a href="/orders/details/<%= order._id %>">View Details</a>
            </div>
        <% }) %>
    <% } else { %>
        <p>No orders found.</p>
    <% } %>
</body>
</html>
